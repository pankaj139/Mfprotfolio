services:
  - type: web
    name: mf-portfolio-api
    runtime: python
    region: singapore
    rootDir: backend
    buildCommand: pip install -r requirements.txt
    pythonVersion: "3.11.9"
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: "3.11.9"
      - key: SECRET_KEY
        generateValue: true          # Render auto-generates a secure random value
      - key: ACCESS_TOKEN_EXPIRE_MINUTES
        value: "1440"
      # DATABASE_URL — set this manually in the Render dashboard to your Supabase
      # connection string (see docs/SETUP_GUIDE.md §2.8).
      # Falls back to SQLite (ephemeral) if not set.
      - key: DATABASE_URL
        value: "sqlite:///./mf_portfolio.db"
    healthCheckPath: /health
    autoDeploy: true
